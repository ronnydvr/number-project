{{
  "name": "coffee-shop-menu",
  "version": "1.0.0",
  "description": "Coffee shop price list",
  "private": true,
  "scripts": {
    "build": "echo 'No build needed for static site'",
    "deploy": "git push origin main"
  }
}
